# =============================================================================
# Subtitle Muxer — manual multi-platform PyInstaller builds (artifacts only)
# Trigger: Actions → Build → Run workflow
# Also callable from the Build and Release workflow.
#
# Artifact examples:
#   subtitle-muxer-windows-0.1.0-portable
#   subtitle-muxer-windows-0.1.0-installable
#   subtitle-muxer-macos-arm64-0.1.0-portable
#   subtitle-muxer-macos-intel-0.1.0-portable
#   subtitle-muxer-ubuntu-0.1.0-portable
#
# Linux/macOS: portable only. Windows: portable + installable.
# =============================================================================
name: Build

on:
  workflow_dispatch:
    inputs:
      python_version:
        description: "Python version (Windows / macOS)"
        required: false
        default: "3.14"
        type: string
  workflow_call:
    inputs:
      python_version:
        description: "Python version (Windows / macOS)"
        required: false
        default: "3.14"
        type: string

permissions:
  contents: read

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Windows + macOS (hosted runners) ─────────────────────────────────────
  build-desktop:
    name: Build (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows
            upload_installable: true
          # Apple Silicon (arm64)
          - os: macos-15
            platform: macos-arm64
            upload_installable: false
          # Intel Mac
          - os: macos-13
            platform: macos-intel
            upload_installable: false

    env:
      SUBTITLE_MUXER_PLATFORM: ${{ matrix.platform }}
      # Linux/mac use portable only; Windows bat ignores this and always builds both.
      SUBTITLE_MUXER_BUILD_KINDS: portable

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ inputs.python_version }}
          cache: pip
          cache-dependency-path: requirements/requirements.txt

      - name: Read app version
        id: version
        shell: bash
        run: |
          VERSION="$(python scripts/read_version.py)"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "App version: ${VERSION}"
          uname -m

      - name: Setup env + build (Windows)
        if: matrix.platform == 'windows'
        shell: cmd
        run: |
          call scripts\setup_env.bat
          if errorlevel 1 exit /b 1
          call scripts\build_app.bat
          if errorlevel 1 exit /b 1

      - name: Setup env + build (macOS)
        if: startsWith(matrix.platform, 'macos')
        run: |
          chmod +x scripts/setup_env.sh scripts/build_app.sh scripts/read_version.py
          ./scripts/setup_env.sh
          ./scripts/build_app.sh

      - name: Upload portable artifact
        uses: actions/upload-artifact@v7
        with:
          name: subtitle-muxer-${{ matrix.platform }}-${{ steps.version.outputs.version }}-portable
          path: dist/${{ matrix.platform }}/${{ steps.version.outputs.version }}/portable/**
          if-no-files-found: error
          retention-days: 30

      - name: Upload installable artifact (Windows only)
        if: matrix.upload_installable
        uses: actions/upload-artifact@v7
        with:
          name: subtitle-muxer-${{ matrix.platform }}-${{ steps.version.outputs.version }}-installable
          path: dist/${{ matrix.platform }}/${{ steps.version.outputs.version }}/installable/**
          if-no-files-found: error
          retention-days: 30

  # ── Linux distros (containers) — portable only ───────────────────────────
  build-linux:
    name: Build (${{ matrix.platform }})
    runs-on: ubuntu-latest
    container:
      image: ${{ matrix.image }}
      options: --user root
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu
            image: ubuntu:24.04
            pkgs: apt
          - platform: debian
            image: debian:bookworm
            pkgs: apt
          - platform: mint
            image: linuxmintd/mint22.1-amd64
            pkgs: apt
          - platform: fedora
            image: fedora:latest
            pkgs: dnf
          - platform: arch
            image: archlinux:latest
            pkgs: pacman

    env:
      SUBTITLE_MUXER_PLATFORM: ${{ matrix.platform }}
      SUBTITLE_MUXER_BUILD_KINDS: portable

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install distro build dependencies
        run: |
          chmod +x scripts/ci_install_linux_deps.sh scripts/setup_env.sh scripts/build_app.sh scripts/read_version.py
          ./scripts/ci_install_linux_deps.sh "${{ matrix.pkgs }}"

      - name: Read app version
        id: version
        run: |
          VERSION="$(python3 scripts/read_version.py)"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "App version: ${VERSION}"

      - name: Setup env
        run: ./scripts/setup_env.sh

      - name: Build with PyInstaller
        run: ./scripts/build_app.sh

      - name: Upload portable artifact
        uses: actions/upload-artifact@v7
        with:
          name: subtitle-muxer-${{ matrix.platform }}-${{ steps.version.outputs.version }}-portable
          path: dist/${{ matrix.platform }}/${{ steps.version.outputs.version }}/portable/**
          if-no-files-found: error
          retention-days: 30
